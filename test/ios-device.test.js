'use strict';

const assert = require('assert');

const Device = require('../lib/ios-device');

describe('lib/ios-device.js', function() {
  it('should be', function() {
    assert.ok(Device);
  });
});
